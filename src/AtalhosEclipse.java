import com.sun.prism.paint.Color;


public class AtalhosEclipse {

	/*
	 * ctrl + espaço -> após digitar o nome traz sugestões de nomes
	 * 
	 * <foreach> + ctrl + espaço -> gera o comando foreach automaticamente
	 * 
	 * <get> + ctrl + espaço -> gera getters automáticos com base em um dado atributo
	 * 
	 * <set> + ctrl + espaço -> geta setters automáticos com base em um dado atributo
	 * 
	 * ctrl + 1 -> * sugere correções para os erros
	 *             * cria classes
	 *             * gera getters e setters
	 *             * faz a criação automática de um objeto à partir do new Objeto()
	 *             * usa-se para remover imports desnecessários
	 *             
	 * ctrl + shift + o -> também remove ou importa classes
	 * 
	 * ctrl + 3 -> busca comandos 
	 *             * ctrl + 3 + <ggas> -> cria getters e setters
	 *             * ctrl + 3 + <gcuf> -> cria construtores usando campos
	 *             * ctrl + 3 + <run> -> roda a aplicação
	 *   
	 * ctrl + m -> esconde o Package Explorer
	 * 
	 * tab -> navega entre os parâmetros do construtor
	 * 
	 * esc -> sai da seleção
	 * 
	 * ctrl + s -> salva alterações
	 * 
	 * <syso> + ctrl + espaço -> cria o método System.out.println()
	 * 
	 * ctrl + f11 -> executa o programa
	 * 
	 * f3 (com a classe selecionada) -> abre o código da classe desejada
	 * 
	 * alternar entre as abas -> ctrl + pageDown -> vai para esquerda 
	 *                           ctrl + pageUp -> vai para direita
	 * 
	 */
	
}
